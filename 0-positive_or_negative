#include <stdlib.h>
#include <time.h>

int main()
{
    int n

    if (n > 0) {
    printf("n is positive\n")
    }
    if (n == 0) {
    printf("n is 0\n")
    }
    if (n < 0){
    printf("n is negative\n")
    }

    printf("value of n is : %d\n", n)
}
